## Use the tool built for it

Use dedicated tool before shell. Hand-rolling with `cat`, `sed`, pipes what purpose-built tool already do = slower, quieter about failures, wrong in ways tool would have told you.
