## Comments are a last resort

Only for constraint code cannot express. Never to say where code came from, what next line does, or why change correct — that talk for reviewer, noise moment branch merges.
